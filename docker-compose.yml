services:
  db:
    image: docker.io/library/postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-moniqo}
      POSTGRES_USER: ${POSTGRES_USER:-moniqo}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-moniqo} -d ${POSTGRES_DB:-moniqo}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - moniqo

  mailpit:
    image: docker.io/axllent/mailpit:latest
    restart: unless-stopped
    ports:
      - "${MAILPIT_UI_PORT:-8025}:8025"
      - "${MAILPIT_SMTP_PORT:-1025}:1025"
    networks:
      - moniqo

  # Stub Dockerfile at apps/backend/Dockerfile — replace with full implementation when backend scaffolding lands
  backend:
    profiles: [app]
    build:
      context: ./apps/backend
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      DATABASE_URL: postgres://${POSTGRES_USER:-moniqo}:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@db:5432/${POSTGRES_DB:-moniqo}?sslmode=disable
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      JWT_EXPIRY: ${JWT_EXPIRY:-24h}
      PORT: "8080"
      SMTP_HOST: mailpit
      SMTP_PORT: "1025"
    ports:
      - "${BACKEND_PORT:-8080}:8080"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s
    depends_on:
      db:
        condition: service_healthy
      mailpit:
        condition: service_started
    networks:
      - moniqo

  # Stub Dockerfile at apps/web/Dockerfile — requires output: standalone in next.config; replace with full implementation when web scaffolding lands.
  # NOTE: NEXT_PUBLIC_API_URL is baked in at image build time by Next.js (NEXT_PUBLIC_* vars are inlined by webpack).
  # Setting it here only affects server-side code; pass it as a build arg when building the image for different environments.
  web:
    profiles: [app]
    build:
      context: ./apps/web
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:8080/api/v1}
    ports:
      - "${WEB_PORT:-3000}:3000"
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - moniqo

volumes:
  db_data:

networks:
  moniqo:
    driver: bridge
