# config

Place deployment-specific configuration here (e.g. a `.env` file for the
backend) before running `scripts/start.sh`. This directory ships empty in
release archives; it exists so operators have a conventional place to drop
environment files without editing anything under `bin/` or `web/`.
